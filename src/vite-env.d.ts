/// <reference types="vite/client" />

// Injetado via `define` em vite.config.ts — carimbo de build usado como
// buster do cache do React Query persistido em IndexedDB.
declare const __APP_BUILD_TIME__: string;
